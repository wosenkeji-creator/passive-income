# 被动收入项目群 — 开发规约

## 这个仓库是什么
Top20 被动收入候选项的**统一开发仓**。一个 monorepo，多个互不依赖的独立产品。
每个产品的目标形态：**一次开发 → 平台代收费 → 后期几乎不参与**。

## 最高优先级规则：决策记录的两层边界

**本项目内的开发决策** → 写 `./DECISION_LOG.md`（本仓库根目录，唯一位置）
适用范围：先做哪个产品、WIP 名额分配、技术选型、放弃/杀掉某个产品、闸门标准变更。

**跨项目的战略决策** → 不写在本仓库任何位置
适用范围：本项目整体是否值得占用注意力、与 V6/V7/Dragon 主线的资源分配、注意力预算调整。
这类结论属于全局层，本仓库只被它约束，不产出它。

- 本仓库**禁止**出现除 `./DECISION_LOG.md` 之外的任何优先级排名、路线图、100天计划、结论文件。
- `STATUS.md` 只记录**客观事实**（做到哪一关、上架没、赚了多少、上次维护时间），不含判断和排序。
- 变更本项目内的方向时：先读 `./DECISION_LOG.md` → 指出冲突 → 确认 → 在其**顶部追加**新条目，旧条目标 `REVOKED` + 日期 + 原因。**不删除**旧条目。

## 反混乱的唯一机制：WIP 限制
**同时处于 G0–G3 之间的产品，最多 2 个。**

这是整个仓库唯一的硬约束。20 个候选项不是 20 条并行开发线，是一个**队列**。
G4 之后的产品不占 WIP 名额（它们已经被动了，这才是能攒到 20 个的原因）。

不许可的做法：为了"先都搭起来"而批量创建 20 个产品目录。目录只在产品进入 G0 时创建。

## 阶段闸门（每个产品必须顺序通过）

| 关 | 名称 | 通过条件 |
|---|---|---|
| G0 | 立项 | `SPEC.md` 写完，且**必含**：分发渠道、单位经济、**放弃条件** |
| G1 | 可跑 | 本地端到端能跑通，有真实输入输出 |
| G2 | 上架 | 已在渠道上线，陌生人能搜到 |
| G3 | 首金 | 收到**陌生人**的第一笔付款（自己/朋友买的不算） |
| G4 | 自转 | 上架后连续 30 天，月维护 < 2 小时，**且渠道规定的响应义务能在单次 15 分钟内履行** |
| G5 | 归档 | 稳态运行，或按放弃条件杀掉 |

**默认放弃条件：G2 之后 90 天内没拿到 G3，杀掉。**
在 SPEC.md 里可以改这个数字，但不能没有。僵尸项目是混乱的唯一来源。

## 目录约定
```
products/pNN-slug/     # NN 对应 Top20 的编号，永不复用、永不重排
  SPEC.md              # 立项文档，G0 产物
  LOG.md               # 只追加，不改写。一行一个事实
  src/ ...             # 产品自己的代码
shared/                # 跨产品复用的代码（第 2 个产品用到时才抽）
_templates/            # SPEC.md / LOG.md 模板
STATUS.md              # 事实台账
```
编号锁定 Top20 序号（如 Apify=p01、Forge=p02、小游戏=p03、扩展=p04）。
**产品被杀掉后编号也不回收**，这样 LOG 和 DECISION_LOG 里的引用永久有效。

## 工程约定
- 语言按渠道定，不追求统一：Apify Actor / 扩展 / Forge → TS+Node；数据处理 → Python。
- **先读后写**。改动前先读现有代码，跟已有风格走。
- 每个产品目录必须能独立运行，不依赖其他产品。
- 只有第 2 个产品真的要用同一段代码时，才抽到 `shared/`。**不提前抽象。**
- 密钥只走 `.env`（已 gitignore），代码里不出现明文。不把密钥值回显到对话里。

## 不要做的事
- 不建内部管理后台、不建 n8n 编排、不建自动化看板 —— **在 3 个产品到 G4 之前一律不做**。
  系统套系统 = 摩擦 = 死亡。管理工具本身会变成第 21 个项目。
- 不为了"以后可能要"写配置项和抽象层。
- 不批量上架 AI 糊出来的内容/游戏/素材。这类是账号级处罚，一次封号全部归零。
- 不做需要冷邮件、需要外呼、需要人工沟通才有收入的产品（违反被动定义）。

### 抓取/数据类产品的硬红线（p01、p13、p16 等适用）

| 红线 | 原因 |
|---|---|
| **绝不用账号登录抓取** | 注册即接受 ToS，合同违约主张直接成立；叠加封号导致的服务中断，「低维护」承诺不可能成立 |
| 不碰社交平台（LinkedIn / Meta / X） | 三家均有实际诉讼记录 |
| 不碰票务 | BOTS Act 是专门针对票务自动化的联邦法律，性质不同于普通抓取 |
| 不碰航司 | 有抓取诉讼记录 |

法律态势本身偏松：登出状态抓公开数据已有对抓取方有利的判决。**但刑事风险降低不等于合同风险降低**，
而合同风险的唯一开关就是「有没有接受过那份合同」——所以第一条红线是其中最重要的。

工程上：**提取逻辑绑语义锚点**（JSON-LD 字段名、schema.org 属性、API 字段），不绑 CSS 选择器。
报警要能区分「结构变了」和「被反爬拦了」，这是两种完全不同的处理方式。
