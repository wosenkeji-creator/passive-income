# p01 — Apify Actors

立项日：2026-08-09
当前阶段：G0（本文档未填完，未过关）

## 一句话
<待填。必须具体到"哪一类用户，跑一次拿到什么数据"。
反例：「一个通用爬虫」。正例：「做 XX 的人，输入一个 XX，拿到结构化的 XX 列表」。>

## 分发渠道
- 渠道：Apify Store
- 自然发现流量来源：Apify Store 站内搜索 + 平台自带买家池。**不需要自己宣传**，这是选它的唯一理由。
- 抽成 / 费用：Creator Plan 约 $1/月，含约 $500 平台用量额度
- 计费模式：**按次计费（pay-per-event / pay-per-result）**，用户跑一次付一次
- 政策风险：中低。主要风险是平台调整分成或计费模型，非账号级封禁风险。

> 选它的三个结构性理由：
> 1. 代码在平台上跑 → **盗版不了**
> 2. 平台代收费、代导流 → 不用建收款也不用做营销
> 3. 同一份逻辑可复用成 p13 付费 MCP Server → 一次开发两个入口

## 单位经济
- 定价模型：按次
- 单价：<待填>
- 边际成本：平台算力 + 代理 IP + 目标站点反爬成本 <待填>
- 毛利：<待填>
- 到 $500/月 需要多少调用量：<待填>

参考基线（调研所得，独立可验证）：新 Actor 三个月做到约 64 用户 / 约 $200 一个月；
头部独立开发者 >$10k MRR。**中位数远低于此，按 $200 规划，不按 $10k 规划。**

## 放弃条件
- G2 后 **90 天**内没拿到 G3（陌生人首笔付款）→ 杀掉，不续命
- 目标站点上反爬导致月维护 > 6 小时且无法通过改代码降下来 → 杀掉
- Apify 取消按次计费或抽成超过 30% → 重新评估

## 维护面
- 必须介入的情况：目标站点改版 / 反爬升级 / 平台 SDK breaking change
- 预计月维护：2–4 小时
- 这个维护量是**护城河**：目标站点改版会淘汰掉不维护的竞争者。
  纯零维护的产品在 2026 年卖不出价钱——这是本次调研最贵的一条结论。

## 范围
做：
- [ ] 选定垂直方向并把「一句话」写实
- [ ] 单 Actor，单一输入 → 结构化输出
- [ ] 按次计费接好
- [ ] Store listing（说明 + 截图 + 示例输入输出）

明确不做：
- 不做多功能聚合 Actor
- 不做需要用户配置一堆参数的
- 不自建收款、不自建落地页

## 复用
- 用到 `shared/`：暂无（首个产品）
- 可能被复用：抓取与解析层 → p13 付费 MCP Server 直接包一层即可
