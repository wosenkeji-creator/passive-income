# DECISION_LOG — 被动收入项目群

## 本文件的管辖范围

**只管本项目内部的开发决策。** 具体包括：

- 20 个候选项里先做哪个、做到什么程度、什么时候杀掉
- WIP 上限、阶段闸门的定义与调整
- 技术选型、渠道选择、收款通道选择
- 某个产品的立项 / 归档 / 击杀决定

**不管、也不许写在这里的：**

- 本项目整体是否值得占用注意力
- 本项目 vs V6 / V7 / Dragon 等交易系统主线的资源分配与优先级
- 跨项目的排序、路线图、100 天计划

以上属于全局战略结论，归 `Desktop\DECISION_LOG.md`（该文件目前尚未创建）。
若某条本项目决策会挤占交易系统的时间预算，**先停下，去全局层面确认，不在本文件里自行决定。**

## 维护规则

- 新决策**追加在最上面**（紧跟本节之后），旧决策原地保留。
- 决策变更时：旧条目标记 `REVOKED` + 日期 + 原因。**不删除。**
- 口头指示与本文件冲突时：先指出冲突，确认后改文件，然后才执行。
- 发现两条决策互相矛盾：这本身是最高优先级问题，先解决矛盾再干活。

---

## 2026-08-10 · D005 · Top20 v2 裁定：合并已证同构候选，主项目换成「工作流回归测试」，p01 归档到 G5

**状态：** ACTIVE

**编号口径（本条及以后一律遵守，不许混用）**

- **legacy pNN** = D001 采用的旧 Top20，即 `TOP20_EVIDENCE.md` 矩阵里的 p01–p20（p01 Apify、p13 付费 MCP Server、p16 pSEO 等）。**产品目录、STATUS.md、WIP 计数只认这套编号。**
- **v1 #N** = `Desktop\gpt5.6 top20- v1版.md` 快照里的第 1–20 名（AI 工作流方向的另一套候选）。**它只是对话参考快照，从未获得编号，也没有产品目录。**
- 两套是**完全不同的清单**，不存在对应关系。D005 之前的版本把 `p01(Apify), 13, 16` 写成一簇，是把 legacy p01 和 v1 #13（AI 输入隐私脱敏）、v1 #16（垂直场景工作流商店）混在一起，**该归类是错的，此处删除并作废**。

**与 D001 的关系：** **D001 REVOKED**（其条目已原地标记，原文保留）。
**与 D002 的关系：** **第 3 条 PARTIALLY SUPERSEDED**。D002 第 3 条原文是「WIP=2 维持，第 2 格保持关闭，等 p01 过 G3 再开」。D005 不等 p01 过 G3 就使用两个名额，**直接推翻这半句**。D002 的第 1、2、4、5 条（G4 口径含 15 分钟响应义务、G3 取平台侧记录、不建监控/汇报层、p01 用 TS+Node）**继续 ACTIVE，不受影响**。
**与 D004 的关系：** 补充，不推翻。D004 保持 ACTIVE，但其适用范围仅限 Apify 提现（见第六节）。

### 一、伪多样化：只合并已有证据证明同构的，不按名称硬凑

先说清楚这一节能证明什么、不能证明什么。**能读到的只有 v1 快照里的一行名称加回本模型，没有任何一个 v1 候选有产品级证据**（`TOP20_EVIDENCE.md` 里 legacy p02–p20 全部是「无候选级证据」）。所以按名称推断「共用同一核心资产」这件事本身是有边界的：名称能证明的是**输出物重叠**，不能证明技术栈相同。

**A. 已证同构，合并（v1 #1、#6、#7）**

| v1 # | 名称 | 判定依据 |
|---|---|---|
| #1 | AI 工作流质量审计与回归监控 SaaS | 名称即含「回归监控」 |
| #6 | n8n/Make 工作流静态审计器 | 「审计」是 #1 的子集，去掉了监控 |
| #7 | Prompt/Agent 回归测试套件 | 「回归测试」是 #1 的子集，换了对象 |

三者的输出物都是「跑一遍 → 与基线比 → 报告差异」，v1 自己也在待评审问题第 6 条承认了这个疑虑。**合并为主项目一个形态。**

**B. 疑似重叠但证据不足，标记后搁置（v1 #5、#8、#11、#12、#14）**

#5（JSON 输出检查修复）、#8（API 成本异常监控）、#11（提示词注入扫描）、#12（错误日志诊断）、#14（合成测试数据生成）——这五个**可能**复用主项目的执行管道，但也**可能**各自需要独立的规则库、威胁库或数据生成逻辑。**现有证据不足以判定，一律标「未裁定」，不合并、不淘汰。** 上一版把它们并入簇 A 是无证据的硬凑，此处撤回。

**C. 明确不同构，前一版归类错误，现更正**

| v1 # | 名称 | 前一版错误归类 | 更正 |
|---|---|---|---|
| #3 | AI 工作流可靠性工具包 | 被塞进「计算器/纯前端」簇 | **不是计算器。**是模板+量表+清单的**内容资产包**（$79 × 14 份/月，一次性交付） |
| #18 | Codex/Claude Code 一人公司操作包 | 同上 | **不是计算器。**同样是内容资产包（$79 × 14 份/月） |
| #4、#17 | 成本计算器/价格数据库、价格历史与变动提醒 | 与 #3、#18 混为一簇 | 这两个才是价格数据类，且**都需要持续维护的价格数据库**，不是「无技术资产纯前端」 |
| #10 | 模型路由配置生成 | 同上 | **未裁定**，与 #4 的关系无证据 |

**「无技术资产、纯前端」这个说法本身是错的**，已删除：#4/#17 需要维护模型价格库，价格变动就是维护面。

**D. 结论：不硬凑四簇**

证据只支持一个动作：**把 v1 #1、#6、#7 合并**。其余候选的重叠关系**未裁定**。不宣称「20 个只有 4 个」——那个数字是按名称推断出来的，没有证据支撑。诚实的说法是：**v1 的 20 个里，能确认同构的是 3 个，剩下 17 个的独立性既未证成也未证伪。**

### 二、legacy p02–p20 的状态与编号规则

- legacy p02–p18：**全部未立项**，`TOP20_EVIDENCE.md` 记为「无候选级证据」，仓库内无产品目录。D001 撤销后，它们**不再享有「已采用候选清单」的地位**，退回为「未经证据审计的想法」。任何一个要启动，须自己走 G0 并单独追加决策。
- legacy p19、p20：调研阶段已否掉，编号不回收。
- legacy p01：见第四节，本条裁定归档到 G5。
- **编号规则：** legacy pNN 永不复用、永不重排。v1 候选若将来立项，**分配新的 legacy 编号（从 p21 起）**，不沿用 v1 #N，避免两套编号再次混淆。

### 三、六个维度的证据强度与缺口

| 维度 | 强度 | 具体证据 | 缺口（不掩盖） |
|---|---|---|---|
| VOC（痛点存在） | **Proxy** | 14 条 Reddit 独立发帖（r/n8n、r/PromptEngineering、r/LLMDevs、r/mlops），主题为回归测试、生产环境漂移、维护痛 | **抱怨 ≠ 付费。**14 条里没有一条提到「我为此付过钱」或「我在找付费方案」。缺少付费访谈 |
| 竞品（市场存在） | **Measured** | GitHub API 实测：promptfoo 24,087★/501 issues、langfuse 32,778★/764、Helicone 6,048★/160、Portkey 12,676★/243、ChainForge 3,024★/69 | star 数只证明「有人关注」，不证明「有付费缺口」。**issues 数没有按「付费用户提的」拆分** |
| 价格锚点 | **Measured** | 直访官网：Langfuse Hobby 免费 / Core $29 / Pro $199；Braintrust $0–$249；Helicone $79–$799；Portkey $49+；promptfoo 永久免费 + Enterprise 面议 | **免费层过强是最大威胁。**promptfoo 永久免费 + langfuse Hobby 免费，直接压掉 $0–$29 区间。锚点存在，但**能不能在免费层之上卖出去，没有任何证据** |
| 获客 | **缺失（无证据）** | — | **这是最大空洞。**没有关键词月搜索量、没有渠道 CAC、没有任何一条「陌生人自己搜到并付费」的样本 |
| 维护成本 | **Estimated** | 提取逻辑绑语义锚点（JSON-LD / schema.org）可降低结构变更频率 | 未实测。**目标是 ≤2h/周，但没有任何一个产品跑满 30 天验证过这个数字** |
| 回本模型 | **Estimated** | 见第五节推算 | 全部是算术，输入参数（转化率、留存）无一实测 |

**把这张表读明白：**证据链在「痛点」和「竞品」两端是 Proxy/Measured，中间的「获客」是纯空洞。**目前不存在任何一条证据支持「陌生人会自己找到我并付款」。** 这是唯一需要在 60 天内证伪或证成的东西，其余都是二阶问题。

### 四、legacy p01 归档到 G5，不搞「冻结」

D003 的实测数据（不是估计）：单价 $0.003/条、开发者分成 80%、$500/月 需约 **208,000 条/月**、而 WTTJ 品类第一名 30 天只有 **117 个用户**。**做到品类第一，量级上也到不了 $500/月**——这是天花板问题，不是执行问题。D001 的错误不在假设「平台自带买家」为假，而在**它把「有买家」当成了「够用的买家」**。

**处置：p01 判定为战略归档，进入 G5。** 代码、SPEC、LOG、编号全部保留在仓库，不删除。

**为什么不是「冻结在 G1」：** `CLAUDE.md` 规定 G0–G3 的产品占 WIP 名额。上一版发明的「冻结不占 WIP」在规约里不存在，而且那正是规约明确要防的东西——**僵尸项目是混乱的唯一来源**。一个停在 G1、不推进也不杀掉的产品就是僵尸。**要么占名额，要么出闸门，没有第三种状态。**

**复活条件：** 若将来认为 Apify 链路仍值得一验，须**另追加一条新决策**重新立项并占用 WIP 名额，不能凭本条自动复活。

### 五、主项目 + 两个试验

**收入目标拆解（Estimated，输入参数全部未实测）**

CNY 5,000/月 净现金流 ≈ USD 700/月 净 ≈ **USD 1,000/月 毛**（扣支付手续费 ~3%、API/服务器 ~$80、退款准备 ~10%、汇率与提现损耗）。

| | 主项目 | 试验 A（付费） | 试验 B（引流） |
|---|---|---|---|
| 是什么 | **n8n / Make 工作流回归测试**（v1 #1+#6+#7 合并形态）：接入工作流定义，定时跑，与基线快照对比，变更时告警 | **工作流文档与 SOP 生成器**（v1 #2）：定义 → 人类可读文档 + 依赖图，一次性付费 | **工作流成本计算器**（v1 #4 的免费子集）：**主项目站内的免费功能页** |
| 独立产品？ | 是，占 1 个 WIP 名额，分配 **legacy p21** | 是，占 1 个 WIP 名额，分配 **legacy p22** | **不是独立产品。**不分配 pNN、不单独进 G0、不单独计 WIP。它是 p21 的一个页面，随 p21 一起过闸门 |
| 客单价 | **$39/月** | **$29 一次性** | $0 |
| 需要多少付费客户 | **26 个活跃订阅**（26 × $39 = $1,014/月毛） | 每月 12 单（$348/月，补足主项目缺口用） | 0 |
| 渠道假设 | n8n 社区论坛 + r/n8n + GitHub README 反向引流。**假设未验证** | Gumroad / Lemon Squeezy 商店内自然搜索 | 站内页面 + Product Hunt/Reddit 引流到 p21 |
| 杀掉条件 | 上架后 **60 天内没有陌生人付费 → 杀**（收紧于 `CLAUDE.md` 的默认 90 天，因免费层威胁强，拖 90 天只是延迟坏消息）。或：promptfoo/langfuse 官方发布同功能 → 立即杀 | 上架后 **90 天内 < 3 单 → 杀**（沿用 `CLAUDE.md` 默认值） | 无独立杀掉条件。若 30 天未给 p21 带来注册 → 下线该页面，属 p21 内部的功能取舍，不是产品击杀 |

**关于试验 B 的口径：** 上一版说它「无后端所以不进 G 闸门、不占 WIP」，那是按技术栈绕过规约，已作废。**正确的处理是把它定性为 p21 的免费获客功能，而不是产品。** 按技术栈决定是否占 WIP 是错的；按「是不是独立产品」决定才对。

**26 个订阅这个数字要看清楚：** 它假设 0 流失。真实场景下月流失 5% 意味着需要持续获客，而获客证据为零。**主项目的真实风险不在「能不能做出来」，在「26 个人从哪来」。**

**WIP 计数（真实）：** p21 + p22 = **2/2，占满**。p01 已归档到 G5，按 `CLAUDE.md`「G4 之后的产品不占 WIP 名额」不再计入。**在 p21 或 p22 出闸门之前，不许启动第三个产品，包括 legacy p02–p20 里的任何一个。**

### 六、放弃 / 复核条件

- 主项目 G2 后 60 天无陌生人付费 → 杀，且**同时**认定 v1 #1/#6/#7 这一簇整体不可做（三者已证同构，一个不行等于三个都不行，不许换个名字再试一遍）。**但这不波及标为「未裁定」的 #5/#8/#11/#12/#14** ——它们与主项目的同构关系没有证据，不能连坐。
- p21、p22 **双双被杀** → 停止本项目群的新增立项，回到全局层重新确认本项目是否值得占用注意力（**这个确认不在本文件做**）。
- 若 6 个月内净现金流未达 CNY 5,000/月，但已有 ≥1 个陌生人付费样本 → 不杀，改为调整定价与渠道；**没有付费样本则一律杀**。

### 七、尚未决定（不要当成已决）

- p21/p22 的收款渠道（Lemon Squeezy vs Stripe vs Gumroad）未定。**D004 的 PayPal→Wise 只适用于 Apify 提现，不自动适用于订阅制**。
- 是否需要海外实体：默认不需要，**未核实订阅制 SaaS 在收款方合规要求上是否与一次性付费不同**。
- 免费层策略未定（是否给免费额度、额度多大）。这是与 promptfoo 永久免费层正面对撞的地方，须单独决定。
- v1 #5、#8、#10、#11、#12、#14 与主项目的技术复用关系：**未裁定**，须有证据后再判。

---

## 2026-08-09 · D001 状态变更 · REVOKED

**REVOKED 日期：** 2026-08-10
**原因：** D001 的核心假设是「20 个候选共享同一个未验证假设：平台自带买家、不需要自己造流量。p01 的首要价值是验证这句话」。D003 已用实测数据完成该验证，结果是**假设部分为真但不够用**——Apify 确实自带买家，但品类第一名 30 天仅 117 用户，$0.003/条的单价下做到第一也到不了 $500/月。D001 据此指定的「p01 先行」失去依据。
**第二点：** D001 采用的「legacy Top20 候选清单」在 `TOP20_EVIDENCE.md` 里除 p01 外全部是「无候选级证据」。撤销后 legacy p02–p20 退回为「未经证据审计的想法」，不再享有「已采用清单」地位。**注意：不宣称它们只是 4 个方向或彼此同构——那种判断没有证据。**

**各条规则的真实来源（不把来源归错）**

| 规则 | 来源 | 现状 |
|---|---|---|
| WIP=2 上限本身 | **`CLAUDE.md`**（「同时处于 G0–G3 之间的产品，最多 2 个」） | 有效，不随 D001 撤销而失效 |
| 默认杀掉条件（G2 后 90 天未达 G3） | **`CLAUDE.md`**（「默认放弃条件」一节） | 有效。D005 对 p21 单独收紧到 60 天，是加严不是放宽 |
| 「第 2 格保持关闭，等 p01 过 G3 再开」 | **D002 第 3 条** | **已由 D005 PARTIALLY SUPERSEDED** |
| G3 取平台侧记录、G4 含 15 分钟响应义务 | **D002 第 1、2 条** | 继续 ACTIVE |
| 「p01 先行」「采用 Top20 清单」 | D001 本条 | **随本条一并 REVOKED** |

上一版 D005 写「WIP=2 与默认 90 天由 D002 独立维持」，**归错了来源**：这两条都出自 `CLAUDE.md`，D002 维持的是「第 2 格关闭」与 G3/G4 口径。此处更正。

---

## 2026-08-09 · D004 · 收款通道先用 PayPal，达标后切 Wise

**状态：** ACTIVE
**与 D001/D002/D003 的关系：** 补充，不推翻。三条都保持 ACTIVE。

### 决定

| 项 | 结果 |
|---|---|
| 当前通道 | **PayPal** |
| 切换目标 | Wise |
| 切换触发条件 | **月收入连续 3 个月 ≥ $100** |
| 账号标识 | 不写在本文件，写 `.env` 的 `PAYOUT_PAYPAL` / `PAYOUT_WISE` |
| 香港汇丰 | 定位为**终点账户**，不作为 Apify 提现通道 |

### 为什么是门槛而不是手续费

| 通道 | Apify 最低提现 |
|---|---|
| PayPal | **$20** |
| 其他（含 Wise / 银行） | **$100** |

叠加「12 个月未达门槛则收入作废」：选 $100 的通道 = 有真实概率攒一年后全部清零。
按 D003 实测的量级（WTTJ 垂类 4 家竞品 30 天用户合计约 182），首年收入大概率是两位数美元。
**$100 门槛在这个量级上不是「晚点拿」，是「拿不到」。**

手续费差距在此量级可忽略：PayPal 跨境收款+换汇约 4%，Wise 约 0.5%，差 3.5% —— 在 $20 上是 $0.7。
为省 $0.7 承担清零风险，方向错误。

银行电汇到汇丰更差：SWIFT 单笔 $15–30，在 $100 提现上是 15–30% 损耗。
Wise 的费率优势依赖它提供的美元本地收款信息（走 ACH 而非 SWIFT）才成立。

### 未核实项（不当成事实）

已核实的只有「PayPal $20 / 其他 $100」这个二分。
**Apify 后台是否明确列出 Wise，未核实。** 大概率以「银行转账」身份被接受（Wise 提供美元收款账号）。
设置提现方式时需顺手确认清单实际列了哪几种 —— 这同时也验证了 $100 那一档具体指什么。

### 与 G3 口径的关联

D002 已把 G3 定为「平台侧记录到收入」，不等到账，**代价是 G3 不再证明钱拿得到手**。
该缺口靠「现在就做 KYC」补。因此**第一笔提现真正到账是独立于 G3 的验证点**，到账当天在 LOG 单独记一行。

---

## 2026-08-09 · D003 · p01 目标定为 Welcome to the Jungle（欧洲职位），单价 $0.003/条

**状态：** ACTIVE
**与 D001/D002 的关系：** 补充，不推翻。两条都保持 ACTIVE。

### 决定

| 项 | 结果 |
|---|---|
| p01 目标 | Welcome to the Jungle（welcometothejungle.com）职位数据 |
| 排除 | mobile.de、bizbuysell（实测 403 AkamaiGHost） |
| 挂起 | handelsregister（本机网络取不到，非站点属性，见下） |
| 单价 | **$0.003/条**（$3/1,000），对齐市场第一名，**不走 SPEC 原写的「区间上半段」** |
| 抓取路径 | sitemap 枚举 + 详情页 JSON-LD，不用 Algolia |

### 证据（实测，非估计）

全部为登出、无代理、纯 HTTP：

| 站点 | 结果 | 判定 |
|---|---|---|
| mobile.de | 403，Server: AkamaiGHost | 排除 |
| bizbuysell | 403，Server: AkamaiGHost | 排除 |
| handelsregister | 两次失败：超时 30s / curl exit=35（TLS） | 挂起 |
| WTTJ | 200，详情页 209 KB | 保留 |

WTTJ 语义锚点**两层都验通**：
- 列表层：`sitemaps/index.xml.gz` → 12 张 job-listings 子图，抽样单图 10,000 条 URL 且 **10,000 条全部带 lastmod** → 全站约 **120,000 个职位 URL**，且 lastmod 可做增量。
- 详情层：JSON-LD `@type=JobPosting`，字段 `title / datePosted / validThrough / employmentType / hiringOrganization / jobLocation / industry / qualifications` 全部实取到。

两层都**不需要住宅代理、不需要无头浏览器、不需要 cookie**。这直接满足 SPEC 的排除规则和「绝不登录」红线。

### 为什么不用 Algolia

站点 `/api/env` 公开暴露 `PUBLIC_ALGOLIA_APPLICATION_ID` 与 client search key，但实测该 key
`listIndexes` 与猜测的 index 名均返回 **403 "Index not allowed with this API key"**，jobs index 名未在 env 或 25 个 JS chunk 中出现。
sitemap + JSON-LD 已经够用，且是官方公开面，比逆向前端接口稳。**不再花时间找 index 名。**

### 单价为什么不听 SPEC 的「上半段」

SPEC 原文要求单价落在 $1–10/1,000 的上半段。实测市场不在上半段：

| 竞品 | 30 天用户 | 实际单价 |
|---|---|---|
| clearpath/welcome-to-the-jungle-jobs-api | 117 | $2.99/1,000 |
| shahidirfan/Jungle-Job-Scraper | 37 | $0.99/1,000 |
| logiover/welcome-to-the-jungle-jobs-scraper | 28 | 未设 result 价 |

市场实际带宽是 **$1–3/1,000**，$5 以上没有活跃样本。作为零评价新号按 $5 定价，是同时输掉价格和信任。
取 $3 与第一名同价。**这是明确偏离 SPEC 写死的规则，已在 SPEC 内标注冲突，不是默默改的。**

因为 WTTJ 不需要住宅代理，边际成本约 $0.00001/条，比价格低两个数量级 —— 原先「$0.001/条必亏」的结论
只在需要住宅代理时成立，**在这个目标上不成立**。价格带底部不再是亏损端。

### 这个决定暴露的真实问题（不掩盖）

按 $0.003/条、开发者分成 80%，**$500/月需要约 208,000 条/月**。
市场第一名 30 天只有 117 个用户。也就是说：**单个 Actor 做到品类第一，量级上也到不了 $500/月。**

这不推翻 p01，但把 D001 里那条待验证前提收窄成了一个更硬的问题：
不是「平台会不会带来买家」，而是「单 Actor 的天花板是否本来就远低于目标」。
G2 后 30 天的陌生人调用量会直接给出答案，**且不靠加 Actor 来救**（SPEC 已写死）。

---

## 2026-08-09 · D002 · G4 措辞改准，G3/WIP 维持，监控层延后，p01 用 TS+Node

**状态：** ACTIVE
**与 D001 的关系：** 补充，不推翻。**D001 保持 ACTIVE，不标 REVOKED。**

**决策内容**

1. **G4 措辞改准**（闸门定义变更，已同步改 `CLAUDE.md`）：
   原「上架后连续 30 天，月维护 < 2 小时」
   → 「上架后连续 30 天，月维护 < 2 小时，**且渠道规定的响应义务能在单次 15 分钟内履行**」。

   理由：Apify Store Publishing Terms §9.1 用户报告须 **14 天内**处理、§9.2 标 urgent 的邮件须
   **3 个工作日内**回、§8.1 自动化测试失败超 **30 天**未修则废弃下架且无补偿、§11.3 故障期间不累计收入。
   **「无人值守」在这个渠道上不成立**，原措辞掩盖了这一点。
   15 分钟是**判定标准，不是承诺值**——单次超过 15 分钟就说明选题的维护面本身有问题。

2. **G3 口径维持，并明确算哪一刻**：G3 =「收到陌生人的第一笔付款」，
   判定时点取**平台侧记录到该笔收入**，不等钱到账。

   理由：§11 提现需先过 KYC（政府身份证 + 地址证明 + 税务文件 + 受益人信息），
   最低提现 PayPal **$20** / 其他 **$100**，连续 12 个月不达标收入作废。
   若按「钱进银行」判定，G3 会被提现门槛绑架，与它的原意（验证有没有陌生人愿意付钱）脱钩。
   代价是 G3 不再顺带证明「钱拿得到手」——**该风险改由「现在就做 KYC」这条行动消化**，不靠闸门兜。

3. **WIP=2 维持，第 2 格保持关闭**，等 p01 过 G3 再开。

4. **不建监控层、不建飞书汇报层。** 触发条件：**3 个产品且跨 ≥2 个平台**之后才重新讨论。

   现在的替代方案（成本近零，且不引入新的失败点）：
   - 打开 **Apify 原生失败通知** —— 平台侧推送，我的代码挂掉时它不会跟着挂
   - **静默失败检测放在 Actor 内部**：每个 200 响应校验条数/结构/schema，不匹配就主动报失败，
     **不返回空壳假装成功**。外部监控只能看到「Actor 跑完了」，内部校验才能看到
     「跑完了但返回的是空壳」——后者才是产生 1 星的那种失败。
   - 告警必须区分「结构变更」与「反爬拦截」，这是两种完全不同的处理方式

   理由：现在监控对象是 **0 个**（p01 未过 G0）；平台已经在推同样的通知，脚本只是二次转运；
   监控改变的是「在哪知道坏了」，不改变「谁去修」，真正降低劳动的是选不易坏的目标（语义锚点）；
   首月必然 $0 的周报会训练我忽略飞书，而那是以后真需要的通道。
   另：`CLAUDE.md` 已规定 3 个产品到 G4 前不建看板，本条与之一致，**不是新发明的标准**。

5. **p01 语言选 TS+Node**，与 `CLAUDE.md` 工程约定一致。

   调研里最好的案例作者（98 个 Actor）用 **Go + 纯 HTTP**，自述算力成本低约一个数量级。
   拆开看：**省算力的主因是「不开无头浏览器」，不是换语言**——这条采纳，写进 p01 工程要求。
   Go 的代价**尚未核实**（Apify 官方 SDK 只有 JS/Python，Go 大概率需自行处理 Actor 生命周期、
   输入输出与数据集推送）。在核实前，不为一个未量化的收益放弃官方 SDK。

**理由（针对被否掉的多路径 + 监控提案）**

该提案与 D001 的立论直接冲突：20 个候选共享**同一个**未验证假设
（「平台自带买家、不需要自己造流量」）。铺 5 条渠道不是分散风险，是**同一个赌注押 5 遍**；
且 5 条渠道 = 4–5 套不同的政策制度（Apify 的 PPE + KYC + 30 天修复钟 vs Chrome 审核 vs
Forge 免佣窗口 vs HTML5 分成），维护面是**乘法不是加法**。
形态上与已被淘汰的多因子同构（alpha 仅 1.5%，动量是唯一有效因子）。

**放弃/复核条件**

- 若 p01 实运行中发现 15 分钟响应口径无法执行（如紧急邮件需要复现问题、单次远超 15 分钟）→
  判定为**选题的维护面问题**，按 SPEC 放弃条件处理，**不放宽 G4**。
- Go 支持程度核实后，若确认有可用路径且算力差距成立 → 可提议改工程约定，须在本文件追加新条目。
- 若在 3 产品跨 2 平台之前就出现「不建监控就会漏掉致命故障」的实例 → 提前重议第 4 条，须追加新条目。

**尚未决定（不要当成已决）**

- p01 具体目标站（2 路深挖 agent 未回，三轴表的「反爬强度」「长期维护成本」仍是估计值）
- p01 单价、边际成本、毛利、到 $500/月 所需调用量
- 飞书层真正建起来时的实现方式（已知坑：Windows curl 中文乱码，须走 PowerShell UTF-8 或从 VPS 发）

---

## 2026-08-09 · D001 · 采用 Top20 候选清单，WIP=2，p01 先行

**状态：** REVOKED（2026-08-10，由 D005 撤销。原因见 D005 文末「D001 状态变更」条目。原文以下保留不改。）

**决策内容**

1. 接受调研产出的 Top20 被动收入候选清单，作为本项目的候选池。编号 `p01`–`p20` 锁定该清单排序，**编号永不重排、永不回收**。
2. **WIP 上限 = 2**：同时处于 G0–G3 的产品最多 2 个。G4 之后不占名额。
3. **首个开工产品：p01 Apify Store Actors。** 第二个名额暂不开放，等 p01 过 G3 再定。
4. 每个产品立项（G0）必须在 SPEC.md 写明三项：分发渠道、单位经济、**放弃条件**。缺一项即未过 G0。
5. 默认放弃条件：G2 上架后 90 天内未达 G3（陌生人首笔付款）→ 杀掉。可在 SPEC 内调整数字，不可缺省。

**理由**

- 20 个候选共享同一个**未验证假设**："平台自带买家、不需要自己造流量"。该假设未验证前并行铺开，等于把同一个赌注押 20 遍。p01 的首要价值是验证这句话，而非其本身收入。
- p01 排第一的实质原因：平台代收费（Apify 自己结算，无需自建收款通道）、按次计费无法盗版、平台自带买家流量，且**代码可套 `shared/mcp-wrap/` 直接复用成 p13 付费 MCP Server**——一份代码两个入口，是 Top20 里唯一的免费杠杆。
- 20 个候选项的最大风险不是做不出来，是攒下一批半死不活的僵尸项目，每个都占一点注意力。因此放弃条件必须**前置**书写。

**放弃/复核条件**

- p01 若在 G2 后 90 天未达 G3 → 判定"平台自带买家"假设不成立，**整份 Top20 排序需要重做**，不是换下一个产品继续试。
- WIP=2 若被证明过紧（p01 长期卡在等审核等外部环节、无事可做）→ 可提议放宽到 3，须在本文件追加新条目。

**尚未决定（不要当成已决）**

- p01 具体做哪个抓取目标
- 第二个 WIP 名额给谁
- `shared/` 下各模块的抽取时机（原则：第 2 个产品用到时才抽）
