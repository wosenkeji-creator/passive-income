# DECISION_LOG — 被动收入项目群

## 本文件的管辖范围

**只管本项目内部的开发决策。** 具体包括：

- 20 个候选项里先做哪个、做到什么程度、什么时候杀掉
- WIP 上限、阶段闸门的定义与调整
- 技术选型、渠道选择、收款通道选择
- 某个产品的立项 / 归档 / 击杀决定

**不管、也不许写在这里的：**

- 本项目整体是否值得占用注意力
- 本项目 vs V6 / V7 / Dragon 等交易系统主线的资源分配与优先级
- 跨项目的排序、路线图、100 天计划

以上属于全局战略结论，归 `Desktop\DECISION_LOG.md`（该文件目前尚未创建）。
若某条本项目决策会挤占交易系统的时间预算，**先停下，去全局层面确认，不在本文件里自行决定。**

## 维护规则

- 新决策**追加在最上面**（紧跟本节之后），旧决策原地保留。
- 决策变更时：旧条目标记 `REVOKED` + 日期 + 原因。**不删除。**
- 口头指示与本文件冲突时：先指出冲突，确认后改文件，然后才执行。
- 发现两条决策互相矛盾：这本身是最高优先级问题，先解决矛盾再干活。

---

## 2026-08-09 · D003 · p01 目标定为 Welcome to the Jungle（欧洲职位），单价 $0.003/条

**状态：** ACTIVE
**与 D001/D002 的关系：** 补充，不推翻。两条都保持 ACTIVE。

### 决定

| 项 | 结果 |
|---|---|
| p01 目标 | Welcome to the Jungle（welcometothejungle.com）职位数据 |
| 排除 | mobile.de、bizbuysell（实测 403 AkamaiGHost） |
| 挂起 | handelsregister（本机网络取不到，非站点属性，见下） |
| 单价 | **$0.003/条**（$3/1,000），对齐市场第一名，**不走 SPEC 原写的「区间上半段」** |
| 抓取路径 | sitemap 枚举 + 详情页 JSON-LD，不用 Algolia |

### 证据（实测，非估计）

全部为登出、无代理、纯 HTTP：

| 站点 | 结果 | 判定 |
|---|---|---|
| mobile.de | 403，Server: AkamaiGHost | 排除 |
| bizbuysell | 403，Server: AkamaiGHost | 排除 |
| handelsregister | 两次失败：超时 30s / curl exit=35（TLS） | 挂起 |
| WTTJ | 200，详情页 209 KB | 保留 |

WTTJ 语义锚点**两层都验通**：
- 列表层：`sitemaps/index.xml.gz` → 12 张 job-listings 子图，抽样单图 10,000 条 URL 且 **10,000 条全部带 lastmod** → 全站约 **120,000 个职位 URL**，且 lastmod 可做增量。
- 详情层：JSON-LD `@type=JobPosting`，字段 `title / datePosted / validThrough / employmentType / hiringOrganization / jobLocation / industry / qualifications` 全部实取到。

两层都**不需要住宅代理、不需要无头浏览器、不需要 cookie**。这直接满足 SPEC 的排除规则和「绝不登录」红线。

### 为什么不用 Algolia

站点 `/api/env` 公开暴露 `PUBLIC_ALGOLIA_APPLICATION_ID` 与 client search key，但实测该 key
`listIndexes` 与猜测的 index 名均返回 **403 "Index not allowed with this API key"**，jobs index 名未在 env 或 25 个 JS chunk 中出现。
sitemap + JSON-LD 已经够用，且是官方公开面，比逆向前端接口稳。**不再花时间找 index 名。**

### 单价为什么不听 SPEC 的「上半段」

SPEC 原文要求单价落在 $1–10/1,000 的上半段。实测市场不在上半段：

| 竞品 | 30 天用户 | 实际单价 |
|---|---|---|
| clearpath/welcome-to-the-jungle-jobs-api | 117 | $2.99/1,000 |
| shahidirfan/Jungle-Job-Scraper | 37 | $0.99/1,000 |
| logiover/welcome-to-the-jungle-jobs-scraper | 28 | 未设 result 价 |

市场实际带宽是 **$1–3/1,000**，$5 以上没有活跃样本。作为零评价新号按 $5 定价，是同时输掉价格和信任。
取 $3 与第一名同价。**这是明确偏离 SPEC 写死的规则，已在 SPEC 内标注冲突，不是默默改的。**

因为 WTTJ 不需要住宅代理，边际成本约 $0.00001/条，比价格低两个数量级 —— 原先「$0.001/条必亏」的结论
只在需要住宅代理时成立，**在这个目标上不成立**。价格带底部不再是亏损端。

### 这个决定暴露的真实问题（不掩盖）

按 $0.003/条、开发者分成 80%，**$500/月需要约 208,000 条/月**。
市场第一名 30 天只有 117 个用户。也就是说：**单个 Actor 做到品类第一，量级上也到不了 $500/月。**

这不推翻 p01，但把 D001 里那条待验证前提收窄成了一个更硬的问题：
不是「平台会不会带来买家」，而是「单 Actor 的天花板是否本来就远低于目标」。
G2 后 30 天的陌生人调用量会直接给出答案，**且不靠加 Actor 来救**（SPEC 已写死）。

---

## 2026-08-09 · D002 · G4 措辞改准，G3/WIP 维持，监控层延后，p01 用 TS+Node

**状态：** ACTIVE
**与 D001 的关系：** 补充，不推翻。**D001 保持 ACTIVE，不标 REVOKED。**

**决策内容**

1. **G4 措辞改准**（闸门定义变更，已同步改 `CLAUDE.md`）：
   原「上架后连续 30 天，月维护 < 2 小时」
   → 「上架后连续 30 天，月维护 < 2 小时，**且渠道规定的响应义务能在单次 15 分钟内履行**」。

   理由：Apify Store Publishing Terms §9.1 用户报告须 **14 天内**处理、§9.2 标 urgent 的邮件须
   **3 个工作日内**回、§8.1 自动化测试失败超 **30 天**未修则废弃下架且无补偿、§11.3 故障期间不累计收入。
   **「无人值守」在这个渠道上不成立**，原措辞掩盖了这一点。
   15 分钟是**判定标准，不是承诺值**——单次超过 15 分钟就说明选题的维护面本身有问题。

2. **G3 口径维持，并明确算哪一刻**：G3 =「收到陌生人的第一笔付款」，
   判定时点取**平台侧记录到该笔收入**，不等钱到账。

   理由：§11 提现需先过 KYC（政府身份证 + 地址证明 + 税务文件 + 受益人信息），
   最低提现 PayPal **$20** / 其他 **$100**，连续 12 个月不达标收入作废。
   若按「钱进银行」判定，G3 会被提现门槛绑架，与它的原意（验证有没有陌生人愿意付钱）脱钩。
   代价是 G3 不再顺带证明「钱拿得到手」——**该风险改由「现在就做 KYC」这条行动消化**，不靠闸门兜。

3. **WIP=2 维持，第 2 格保持关闭**，等 p01 过 G3 再开。

4. **不建监控层、不建飞书汇报层。** 触发条件：**3 个产品且跨 ≥2 个平台**之后才重新讨论。

   现在的替代方案（成本近零，且不引入新的失败点）：
   - 打开 **Apify 原生失败通知** —— 平台侧推送，我的代码挂掉时它不会跟着挂
   - **静默失败检测放在 Actor 内部**：每个 200 响应校验条数/结构/schema，不匹配就主动报失败，
     **不返回空壳假装成功**。外部监控只能看到「Actor 跑完了」，内部校验才能看到
     「跑完了但返回的是空壳」——后者才是产生 1 星的那种失败。
   - 告警必须区分「结构变更」与「反爬拦截」，这是两种完全不同的处理方式

   理由：现在监控对象是 **0 个**（p01 未过 G0）；平台已经在推同样的通知，脚本只是二次转运；
   监控改变的是「在哪知道坏了」，不改变「谁去修」，真正降低劳动的是选不易坏的目标（语义锚点）；
   首月必然 $0 的周报会训练我忽略飞书，而那是以后真需要的通道。
   另：`CLAUDE.md` 已规定 3 个产品到 G4 前不建看板，本条与之一致，**不是新发明的标准**。

5. **p01 语言选 TS+Node**，与 `CLAUDE.md` 工程约定一致。

   调研里最好的案例作者（98 个 Actor）用 **Go + 纯 HTTP**，自述算力成本低约一个数量级。
   拆开看：**省算力的主因是「不开无头浏览器」，不是换语言**——这条采纳，写进 p01 工程要求。
   Go 的代价**尚未核实**（Apify 官方 SDK 只有 JS/Python，Go 大概率需自行处理 Actor 生命周期、
   输入输出与数据集推送）。在核实前，不为一个未量化的收益放弃官方 SDK。

**理由（针对被否掉的多路径 + 监控提案）**

该提案与 D001 的立论直接冲突：20 个候选共享**同一个**未验证假设
（「平台自带买家、不需要自己造流量」）。铺 5 条渠道不是分散风险，是**同一个赌注押 5 遍**；
且 5 条渠道 = 4–5 套不同的政策制度（Apify 的 PPE + KYC + 30 天修复钟 vs Chrome 审核 vs
Forge 免佣窗口 vs HTML5 分成），维护面是**乘法不是加法**。
形态上与已被淘汰的多因子同构（alpha 仅 1.5%，动量是唯一有效因子）。

**放弃/复核条件**

- 若 p01 实运行中发现 15 分钟响应口径无法执行（如紧急邮件需要复现问题、单次远超 15 分钟）→
  判定为**选题的维护面问题**，按 SPEC 放弃条件处理，**不放宽 G4**。
- Go 支持程度核实后，若确认有可用路径且算力差距成立 → 可提议改工程约定，须在本文件追加新条目。
- 若在 3 产品跨 2 平台之前就出现「不建监控就会漏掉致命故障」的实例 → 提前重议第 4 条，须追加新条目。

**尚未决定（不要当成已决）**

- p01 具体目标站（2 路深挖 agent 未回，三轴表的「反爬强度」「长期维护成本」仍是估计值）
- p01 单价、边际成本、毛利、到 $500/月 所需调用量
- 飞书层真正建起来时的实现方式（已知坑：Windows curl 中文乱码，须走 PowerShell UTF-8 或从 VPS 发）

---

## 2026-08-09 · D001 · 采用 Top20 候选清单，WIP=2，p01 先行

**状态：** ACTIVE

**决策内容**

1. 接受调研产出的 Top20 被动收入候选清单，作为本项目的候选池。编号 `p01`–`p20` 锁定该清单排序，**编号永不重排、永不回收**。
2. **WIP 上限 = 2**：同时处于 G0–G3 的产品最多 2 个。G4 之后不占名额。
3. **首个开工产品：p01 Apify Store Actors。** 第二个名额暂不开放，等 p01 过 G3 再定。
4. 每个产品立项（G0）必须在 SPEC.md 写明三项：分发渠道、单位经济、**放弃条件**。缺一项即未过 G0。
5. 默认放弃条件：G2 上架后 90 天内未达 G3（陌生人首笔付款）→ 杀掉。可在 SPEC 内调整数字，不可缺省。

**理由**

- 20 个候选共享同一个**未验证假设**："平台自带买家、不需要自己造流量"。该假设未验证前并行铺开，等于把同一个赌注押 20 遍。p01 的首要价值是验证这句话，而非其本身收入。
- p01 排第一的实质原因：平台代收费（Apify 自己结算，无需自建收款通道）、按次计费无法盗版、平台自带买家流量，且**代码可套 `shared/mcp-wrap/` 直接复用成 p13 付费 MCP Server**——一份代码两个入口，是 Top20 里唯一的免费杠杆。
- 20 个候选项的最大风险不是做不出来，是攒下一批半死不活的僵尸项目，每个都占一点注意力。因此放弃条件必须**前置**书写。

**放弃/复核条件**

- p01 若在 G2 后 90 天未达 G3 → 判定"平台自带买家"假设不成立，**整份 Top20 排序需要重做**，不是换下一个产品继续试。
- WIP=2 若被证明过紧（p01 长期卡在等审核等外部环节、无事可做）→ 可提议放宽到 3，须在本文件追加新条目。

**尚未决定（不要当成已决）**

- p01 具体做哪个抓取目标
- 第二个 WIP 名额给谁
- `shared/` 下各模块的抽取时机（原则：第 2 个产品用到时才抽）
